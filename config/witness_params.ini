# Mode: witness
# numUsers: integer
numUsers=100

# postProbPerStep: double (per user per step)
postProbPerStep=0.02

# simDuration: double (time units)
simDuration=100.0

# minDelay, maxDelay: double
minDelay=1.0
maxDelay=5.0

# maxWitnesses: integer
maxWitnesses=3

# seed: unsigned int
seed=1337

# outputPath: path to CSV output
outputPath=../data/witness_results.csv
